apiVersion: v1
kind: Service
metadata:
  name: connection-limit-app
  namespace: default
  labels:
    app: connection-limit-app
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
  selector:
    app: connection-limit-app