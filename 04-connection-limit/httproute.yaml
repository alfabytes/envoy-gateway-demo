# Alternative configuration using Backend resource
# Use this if you need to reference services using Backend resources
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: connection-limit-app-httproute
  namespace: default
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: eg
      namespace: envoy-gateway-system
  hostnames:
    - "connection-limit-app.alfabytes.xyz"
  rules:
    - backendRefs:
        - group: gateway.envoyproxy.io
          kind: Backend
          name: connection-limit-app-backend
          port: 3000
      matches:
        - path:
            type: PathPrefix
            value: /
---
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: Backend
metadata:
  name: connection-limit-app-backend
  namespace: default
spec:
  endpoints:
    - fqdn:
        hostname: connection-limit-app.default.svc.cluster.local
        port: 3000