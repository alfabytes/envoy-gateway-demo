apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-routing-app
  namespace: default
  labels:
    app: backend-routing-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend-routing-app
  template:
    metadata:
      labels:
        app: backend-routing-app
    spec:
      containers:
      - name: backend-routing-app
        image: hashicorp/http-echo:latest
        args:
          - -text="Hello from Backend Routing App! This is a simple backend-routing application using Envoy Gateway."
          - -listen=:8080
        ports:
        - name: http
          containerPort: 8080
          protocol: TCP
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 200m
            memory: 256Mi
        livenessProbe:
          httpGet:
            path: /
            port: http
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: http
          initialDelaySeconds: 5
          periodSeconds: 5