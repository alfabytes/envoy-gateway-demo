# Alternative configuration using Backend resource
# Use this if you need to reference services using Backend resources
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: backend-routing-app-httproute
  namespace: default
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: eg
      namespace: envoy-gateway-system
  hostnames:
    - "backend-routing-app.alfabytes.xyz"
  rules:
    - backendRefs:
        - group: gateway.envoyproxy.io
          kind: Backend
          name: backend-routing-app-backend
          port: 8080
      matches:
        - path:
            type: PathPrefix
            value: /
---
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: Backend
metadata:
  name: backend-routing-app-backend
  namespace: default
spec:
  endpoints:
    - fqdn:
        hostname: backend-routing-app.default.svc.cluster.local
        port: 8080