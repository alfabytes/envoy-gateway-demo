apiVersion: v1
kind: Service
metadata:
  name: backend-routing-app
  namespace: default
  labels:
    app: backend-routing-app
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: backend-routing-app