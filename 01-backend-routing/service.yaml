apiVersion: v1
kind: Service
metadata:
  name: backend-routing-app
  namespace: default
  labels:
    app: backend-routing-app
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
  selector:
    app: backend-routing-app