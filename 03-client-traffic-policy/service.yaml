apiVersion: v1
kind: Service
metadata:
  name: client-traffic-policy-app
  namespace: default
  labels:
    app: client-traffic-policy-app
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
  selector:
    app: client-traffic-policy-app