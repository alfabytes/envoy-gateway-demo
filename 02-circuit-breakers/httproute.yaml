# Alternative configuration using Backend resource
# Use this if you need to reference services using Backend resources
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: circuit-breakers-app-httproute
  namespace: default
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: eg
      namespace: envoy-gateway-system
  hostnames:
    - "circuit-breakers-app.alfabytes.xyz"
  rules:
    - backendRefs:
        - group: gateway.envoyproxy.io
          kind: Backend
          name: circuit-breakers-app-backend
          port: 8080
      matches:
        - path:
            type: PathPrefix
            value: /
---
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: Backend
metadata:
  name: circuit-breakers-app-backend
  namespace: default
spec:
  endpoints:
    - fqdn:
        hostname: circuit-breakers-app.default.svc.cluster.local
        port: 8080